# Domaine public — HTTPS automatique (Let's Encrypt)
# Variable DOMAIN injectée par docker-compose.prod.yml depuis .env

{$DOMAIN} {
	encode gzip

	reverse_proxy client:80

	header {
		X-Content-Type-Options nosniff
		Referrer-Policy strict-origin-when-cross-origin
		-Server
	}
}
