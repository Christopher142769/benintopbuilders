# Client — variables de build production
# En Docker, ces valeurs sont passées via docker-compose.prod.yml (build args).
# Même origine : le navigateur appelle /api et Socket.io sur le domaine public.

VITE_API_URL=/api
VITE_SOCKET_URL=
VITE_APP_NAME=Bénin Top Builders
