# Client — variables de build production
# Sur Hostinger : définir VITE_* dans hPanel Environment Variables (avant le build).
# En local : cp client/.env.production.example client/.env.production

VITE_API_URL=/api
VITE_SOCKET_URL=
VITE_APP_NAME=Bénin Top Builders
